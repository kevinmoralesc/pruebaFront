name: Prueba de Humo

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout del código
        uses: actions/checkout@v2

      - name: Instalar Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '16'

      - name: Instalar dependencias
        run: npm install

      - name: Descargar ChromeDriver
        run: |
          wget https://chromedriver.chromium.org/LATEST_RELEASE/chromedriver_linux64.zip
          unzip chromedriver_linux64.zip

      - name: Establecer ruta de ChromeDriver
        run: |
          echo "##vso=$(pwd)/chromedriver" >> $GITHUB_ENV

      - name: Ejecutar prueba de humo
        run: |
          export CHROME_BIN=/usr/bin/google-chrome --headless
          node smoke_test.js
